import React from 'react';

import logo from '../images/logo.png';

const Brand = () => (
    <div className="main__brand-wrapper">
        <img className="main__brand" src={ logo }/>
    </div>
);

export default Brand;